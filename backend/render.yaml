services:
  - type: web
    name: patron-dijital-asistan-api
    env: python
    runtime: python-3.11.0
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
